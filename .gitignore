node_modules
dist
dist-ssr
dist-electron
dist-electron-loja
# Instalador Windows copiado por `npm run stage:win-installer` (binário grande; não versionar).
public/install/*.exe
# Ícones e BMPs gerados para NSIS / instalador Windows (regenerar com npm run generate-installer-win-assets)
build/icon.ico
build/icon.png
build/installerSidebar.bmp
build/installerHeader.bmp
# Versão do instalador TI (gerados por scripts/generate-installer-version.mjs)
build/installer-version.nsh
build/installer-version.txt
build/installer-version.meta.json
*.local
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.local
.env.*.local
*.tsbuildinfo

# Só no pacote loja — nunca no instalador TI (ver scripts/stage-loja-pack-flag.mjs)
electron/loja-pack.flag

# Arquivos grandes gerados por `npm run backup:zip` (guardar no Drive, não no git).
backups/

# Keystore TWA / Play (não versionar)
mobile/android.keystore
