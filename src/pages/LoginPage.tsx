/**
 * Re-export do shell desktop — compatibilidade com imports antigos.
 * Rotas: use `@/shells/desktop/pages/LoginPage` (PC) ou `@/shells/mobile/pages/LoginPage` (touch).
 */
export { LoginPage } from '@/shells/desktop/pages/LoginPage';
