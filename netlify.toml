[build]
  command = "npm run build"
  publish = "dist"

# React Router + PWA — sem isso um F5 numa rota que não é / vira 404 no Netlify
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
